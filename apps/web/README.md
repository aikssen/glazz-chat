# Glazz Web

Next.js App Router frontend for Glazz. Run it from the repository root with
`pnpm dev`; use `pnpm check` for the fast presubmit and `pnpm e2e` for the browser
smoke test.

Only `NEXT_PUBLIC_` variables are available to browser code. Provider credentials
belong exclusively to the Go backend.
